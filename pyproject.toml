[project]
name = "audio-to-notes"
version = "0.1.0"
description = "audio-to-notes"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "debugpy>=1.8.21",
    "openai>=3.2.0",
    "openai-whisper>=20250625",
    "pyannote-audio>=4.0.7",
    "pydub>=0.25.1",
    "python-dotenv>=1.2.3",
    "torch>=2.13.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
